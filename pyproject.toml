[project]
name = "study"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-awesome-nav>=3.0",
    "mkdocs-callouts>=1.16.1",
    "mkdocs-material>=9.7",
]

[tool.setuptools]
packages = []          # 문서 저장소라 파이썬 패키지가 없음. pip install . 은 의존성 설치 용도
